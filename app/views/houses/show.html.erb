<!-- show.html.erb -->
<%= cl_image_tag @house.photo, height: 300, width: 400, crop: :fill %>
  <%= @house.title %>
  <%= @house.address %>

<h1>Description</h1>
<%= @house.description %>

<div class="booking_creation_form">
<%= render 'shared/form_bookings' %>
</div>
